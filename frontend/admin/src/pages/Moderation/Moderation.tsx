import React from 'react';

const Moderation: React.FC = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold" style={{ color: 'var(--text-h)' }}>Модерация</h1>
      <p style={{ color: 'var(--text)' }}>Страница модерации комментариев</p>
    </div>
  );
};

export default Moderation;